### table for comments: ###
CREATE TABLE comments (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    course_id INT NOT NULL,
    comment TEXT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (course_id) REFERENCES course(id)
); 
///then added thes forefin keys delte the comments based only course_id
ALTER TABLE comments
ADD CONSTRAINT fk_comments_course
FOREIGN KEY (course_id)
REFERENCES courses(id)
ON DELETE CASCADE;

### table for comments: ###

#### table for badge setter###

ALTER TABLE courses
ADD created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
ADD views INT DEFAULT 0,
ADD enroll_count INT DEFAULT 0;

#### table for badge setter###

__________users table____ 1  USE THIS TABLE________
CREATE TABLE users (
    id INT(11) NOT NULL AUTO_INCREMENT,
    name VARCHAR(150) NULL,
    email VARCHAR(150) NULL,
    password VARCHAR(255) NULL,
    role VARCHAR(20) NULL DEFAULT 'user',
    PRIMARY KEY (id),
    UNIQUE KEY email (email)
) ENGINE=InnoDB
DEFAULT CHARSET=utf8mb4
COLLATE=utf8mb4_general_ci;

__________users table____ 2________
CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(150) NOT NULL,
    email VARCHAR(150) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,

    -- AUTH / SECURITY
    role ENUM('user','admin') NOT NULL DEFAULT 'user',

    -- DISPLAY / PROFILE
    profile_title VARCHAR(100) DEFAULT NULL,

    -- OPTIONAL FEATURES
    remember_token VARCHAR(64) DEFAULT NULL,
    reset_token VARCHAR(64) DEFAULT NULL,
    reset_expires DATETIME DEFAULT NULL,

    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
 >>>> table users fevorite course list:
 CREATE TABLE favorites (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    course_id INT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,

    UNIQUE KEY uniq_user_course (user_id, course_id),

    INDEX idx_user (user_id),
    INDEX idx_course (course_id),

    FOREIGN KEY (course_id) REFERENCES courses(id) ON DELETE CASCADE
);


--------------------
Enroll requist table:
CREATE TABLE enroll_requests (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    course_id INT NOT NULL,
    name VARCHAR(150),
    email VARCHAR(150),
    start_date DATE,
    status ENUM('pending','approved','rejected') DEFAULT 'pending',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);//----------------
ALTER TABLE enroll_requests
ADD UNIQUE KEY uniq_user_course (user_id, course_id);

////---------errir table: ------
Create table error(
id int not null primary key AUTO_INCREMENT comment 'primary key', date_time datetime,
    ip VARCHAR(15), [ip address //not write this part]
    error_code integer,
    error varchar(500),
    file varchar(300),
    line integer,
    site_usr_adm varchar(10)
    ) DEFAULT CHARSET=utf8mb4 COMMENT='';
