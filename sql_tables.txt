### table for comments: ###
CREATE TABLE comments (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    course_id INT NOT NULL,
    comment TEXT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (course_id) REFERENCES course(id)
);
### table for comments: ###

#### table for badge setter###

ALTER TABLE courses
ADD created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
ADD views INT DEFAULT 0,
ADD enroll_count INT DEFAULT 0;

#### table for badge setter###

__________users table____ 1  USE THIS TABLE________
CREATE TABLE users (
    id INT(11) NOT NULL AUTO_INCREMENT,
    name VARCHAR(150) NULL,
    email VARCHAR(150) NULL,
    password VARCHAR(255) NULL,
    role VARCHAR(20) NULL DEFAULT 'user',
    PRIMARY KEY (id),
    UNIQUE KEY email (email)
) ENGINE=InnoDB
DEFAULT CHARSET=utf8mb4
COLLATE=utf8mb4_general_ci;

__________users table____ 2________
CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(150) NOT NULL,
    email VARCHAR(150) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,

    -- AUTH / SECURITY
    role ENUM('user','admin') NOT NULL DEFAULT 'user',

    -- DISPLAY / PROFILE
    profile_title VARCHAR(100) DEFAULT NULL,

    -- OPTIONAL FEATURES
    remember_token VARCHAR(64) DEFAULT NULL,
    reset_token VARCHAR(64) DEFAULT NULL,
    reset_expires DATETIME DEFAULT NULL,

    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
